const root=document.querySelector('#app');
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const api=async(path,options={})=>{const r=await fetch(path,{credentials:'include',cache:'no-store',...options});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`Erro ${r.status}`);return d};
const authUrl='https://nexaurenstory.com/pages/login';let authState=null;let billingSubscription=null;
async function getAuth(){try{const d=await api('/api/auth/me');authState=d.user||null;return authState}catch{authState=null;return null}}
function authLinks(next){const q=encodeURIComponent(next||location.hash||'#/pricing');return `<div class="auth-actions"><a class="btn primary" href="#/auth?next=${q}">Entrar / Criar conta</a><a class="btn secondary" href="${authUrl}" target="_blank" rel="noopener">Abrir Nexauren</a></div>`}
function layout(content,active='home'){const logged=!!authState;root.innerHTML=`<header class="topbar"><a class="brand" href="#/">Nexa<span>uren</span></a><button class="menu-toggle" id="menu-toggle" aria-label="Abrir menu">☰</button><nav class="nav" id="main-nav"><a class="${active==='home'?'active':''}" href="#/">Início</a><a class="${active==='dashboard'?'active':''}" href="#/dashboard">Dashboard</a><a class="${active==='pricing'?'active':''}" href="#/pricing">Planos</a><a class="${active==='faq'?'active':''}" href="#/faq">FAQ</a><a class="${active==='account'?'active':''}" href="#/account">Minha conta</a></nav><div class="top-auth">${logged?`<span class="user-chip">${esc(authState.username||authState.email)}</span><button class="nav-btn" id="logout">Sair</button>`:`<a class="nav-btn" href="#/auth">Entrar</a>`}</div></header><main class="shell">${content}</main><footer class="footer"><div class="footer-grid"><div><a class="brand" href="#/">Nexa<span>uren</span></a><p>Website Builder para criar, publicar e gerir os seus projetos digitais.</p></div><div><strong>Produto</strong><a href="#/dashboard">Dashboard</a><a href="#/pricing">Planos</a><a href="#/faq">FAQ</a></div><div><strong>Conta</strong><a href="#/account">Minha conta</a><a href="#/auth">Entrar / Criar conta</a></div><div><strong>Legal</strong><a href="#/privacy">Privacidade</a><a href="#/terms">Termos</a></div></div><div class="footer-bottom"><span>© ${new Date().getFullYear()} Nexauren</span><span>Uma única conta Nexauren.</span></div></footer>`;const l=document.querySelector('#logout');if(l)l.onclick=logout;const mt=document.querySelector('#menu-toggle'),nav=document.querySelector('#main-nav');if(mt)mt.onclick=()=>nav.classList.toggle('open')}
async function logout(){try{await api('/api/auth/logout',{method:'POST',headers:{'content-type':'application/json'},body:'{}'})}finally{authState=null;billingSubscription=null;location.hash='#/';route()}}
function home(){layout(`<section class="hero"><div class="eyebrow">Website Builder</div><h1>Crie o seu website sem começar do zero.</h1><p>Crie websites, blogs, portfólios, lojas, landing pages e projetos personalizados com o ecossistema Nexauren.</p><div class="actions">${authState?`<a class="btn primary" href="#/dashboard">Abrir Dashboard</a>`:authLinks('#/dashboard')}<a class="btn secondary" href="#/pricing">Ver planos</a></div></section><section class="section"><h2>Uma única conta Nexauren</h2><div class="grid"><article class="card"><h3>Conta central</h3><p class="muted">Entre com a sua conta Nexauren. O Builder não cria uma segunda identidade.</p></article><article class="card"><h3>Billing central</h3><p class="muted">Planos, subscrições e pagamentos continuam ligados ao mesmo sistema Nexauren.</p></article><article class="card"><h3>Websites</h3><p class="muted">Os seus projetos ficam no Builder, associados ao ID da mesma conta Nexauren.</p></article></div></section>`)}
async function pricing(){layout('<div class="loading">A verificar a sua conta...</div>','pricing');const u=await getAuth();if(!u){billingSubscription=null;layout(`<section class="auth-gate"><div class="eyebrow">Nexauren Account</div><h1>Entre para ver os planos.</h1><p>Os planos do Website Builder são os mesmos planos do Nexauren. Entre ou crie a sua conta para carregar o catálogo.</p>${authLinks('#/pricing')}<p class="auth-hint">Não será criada uma conta separada para o Website Builder.</p></section>`,'pricing');return}try{const [d,b]=await Promise.all([api('/api/billing/catalog'),api('/api/billing/account')]);billingSubscription=b.subscription||null;const plans=d.plans||[],current=b.account?.plan_id||'free',hasSubscription=!!billingSubscription?.provider_subscription_id;layout(`<section class="hero compact"><div class="eyebrow">Nexauren Billing</div><h1>Escolha o seu plano.</h1><p>Olá, ${esc(u.username||u.email)}. Estes planos vêm diretamente do catálogo de billing Nexauren.</p></section><section class="current-plan card"><span class="muted">Plano atual</span><strong>${esc(b.account?.plan_name||current)}</strong><span>${Number(b.account?.credits_per_cycle||0).toLocaleString()} créditos por ciclo</span></section><section class="grid">${plans.length?plans.map(p=>{const isCurrent=p.id===current;return `<article class="card plan-card ${isCurrent?'selected':''}"><div class="plan-head"><h3>${esc(p.name)}</h3>${isCurrent?'<span class="status">Atual</span>':''}</div><div class="price">${(Number(p.price_minor||0)/100).toFixed(2)} <small>${esc(p.currency)}${p.billing_interval&&p.billing_interval!=='none'?` / ${esc(p.billing_interval)}`:''}</small></div><p class="muted">${Number(p.credits_per_cycle||0).toLocaleString()} créditos por ciclo</p><button class="btn ${isCurrent?'secondary':'primary'}" data-plan="${esc(p.id)}" data-current="${isCurrent}">${isCurrent?'Plano atual':(hasSubscription?'Atualizar plano':'Comprar')}</button></article>`}).join(''):'<div class="card"><h3>Nenhum plano disponível</h3><p class="muted">O catálogo Nexauren não tem planos ativos neste momento.</p></div>'}</section><p class="billing-note">Se já tiver uma subscrição ativa, use <strong>Atualizar plano</strong> para trocar para o novo plano sem criar uma segunda subscrição.</p>`,'pricing');document.querySelectorAll('[data-plan]').forEach(b=>b.onclick=()=>{if(b.dataset.current!=='true')startPlanAction(b.dataset.plan)})}catch(e){billingSubscription=null;layout(`<section class="error"><h3>Não foi possível carregar os planos.</h3><p>${esc(e.message)}</p></section>`,'pricing')}}
async function startPlanAction(plan){try{const active=!!billingSubscription?.provider_subscription_id;const path=active?'/api/billing/subscription/change':'/api/billing/checkout';const d=await api(path,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({plan_id:plan,product_id:plan})});const url=d?.approval_url||d?.checkout?.approval_url||d?.checkout?.url||d?.checkout?.links?.find?.(x=>x.rel==='approve')?.href;if(url)location.href=url;else{alert(active?'Atualização iniciada. Confirme no PayPal para aplicar o novo plano.':'Compra iniciada.');location.hash='#/account'}}catch(e){alert(e.message)}}
async function authPage(){const params=new URLSearchParams(location.hash.split('?')[1]||''),next=params.get('next')||'#/pricing';if(await getAuth()){location.hash=next;return}layout(`<section class="auth-page"><div class="auth-card"><div class="eyebrow">Nexauren Account</div><h1 id="auth-title">Entrar na sua conta</h1><p id="auth-description">Use a mesma conta Nexauren para o Website Builder.</p><div class="auth-tabs"><button class="tab active" id="login-tab">Entrar</button><button class="tab" id="register-tab">Criar conta</button></div><div id="auth-message" class="auth-message"></div><form id="login-form"><label>Email<input id="login-email" type="email" autocomplete="email" required></label><label>Senha<input id="login-password" type="password" autocomplete="current-password" required></label><button class="btn primary full" type="submit">Entrar</button></form><form id="register-form" hidden><label>Username<input id="register-username" type="text" minlength="3" maxlength="32" required></label><label>Email<input id="register-email" type="email" required></label><label>Senha<input id="register-password" type="password" minlength="8" required></label><button class="btn primary full" type="submit">Criar conta</button></form><p class="auth-hint">Esta autenticação usa diretamente a conta Nexauren. Não existe um utilizador duplicado no Builder.</p></div></section>`,'');const lt=document.querySelector('#login-tab'),rt=document.querySelector('#register-tab'),lf=document.querySelector('#login-form'),rf=document.querySelector('#register-form'),title=document.querySelector('#auth-title'),desc=document.querySelector('#auth-description'),msg=document.querySelector('#auth-message');const mode=r=>{lt.classList.toggle('active',!r);rt.classList.toggle('active',r);lf.hidden=r;rf.hidden=!r;title.textContent=r?'Criar conta Nexauren':'Entrar na sua conta';desc.textContent=r?'Crie a sua conta Nexauren e use-a também no Website Builder.':'Use a mesma conta Nexauren para o Website Builder.';msg.textContent='';msg.className='auth-message'};lt.onclick=()=>mode(false);rt.onclick=()=>mode(true);lf.onsubmit=async e=>{e.preventDefault();const b=lf.querySelector('button');b.disabled=true;msg.textContent='A entrar...';try{await api('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:document.querySelector('#login-email').value.trim(),password:document.querySelector('#login-password').value})});await getAuth();location.hash=next}catch(x){msg.textContent=x.message;msg.className='auth-message error';b.disabled=false}};rf.onsubmit=async e=>{e.preventDefault();const b=rf.querySelector('button');b.disabled=true;msg.textContent='A criar a conta...';try{await api('/api/auth/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username:document.querySelector('#register-username').value.trim(),email:document.querySelector('#register-email').value.trim(),password:document.querySelector('#register-password').value})});await getAuth();location.hash=next}catch(x){msg.textContent=x.message;msg.className='auth-message error';b.disabled=false}}}
async function dashboard(){layout('<div class="loading">A carregar o Dashboard...</div>','dashboard');const u=await getAuth();if(!u){layout(`<section class="auth-gate"><div class="eyebrow">Dashboard</div><h1>Entre para gerir os seus websites.</h1><p>O Dashboard mostra apenas os projetos associados à sua conta Nexauren.</p>${authLinks('#/dashboard')}</section>`,'dashboard');return}try{const [pr,b]=await Promise.all([api('/api/projects'),api('/api/billing/account')]),projects=pr.projects||[];layout(`<section class="hero compact"><div class="eyebrow">Dashboard</div><h1>Olá, ${esc(u.username||'criador')}.</h1><p>Gerencie os seus websites, plano e próximos passos num único lugar.</p><div class="actions"><button class="btn primary" id="new-site">+ Criar website</button><a class="btn secondary" href="#/pricing">Gerir plano</a></div></section><section class="grid stats"><article class="card"><span class="muted">Websites</span><strong class="stat-value">${projects.length}</strong></article><article class="card"><span class="muted">Plano</span><strong class="stat-value small">${esc(b.account?.plan_name||'Free')}</strong></article><article class="card"><span class="muted">Créditos/ciclo</span><strong class="stat-value">${Number(b.account?.credits_per_cycle||0).toLocaleString()}</strong></article></section><section class="section"><div class="section-title"><div><h2>Os meus websites</h2><p class="muted">Projetos associados à sua conta Nexauren.</p></div><button class="btn secondary" id="new-site-2">+ Novo website</button></div><div class="grid">${projects.length?projects.map(p=>`<article class="card project-card"><div class="project-icon">W</div><div><h3>${esc(p.name)}</h3><p class="muted">/${esc(p.slug)} · ${esc(p.status||'draft')}</p></div><div class="project-actions"><button class="btn primary" disabled>Editar</button><button class="btn secondary" disabled>Configurar</button></div></article>`).join(''):`<div class="card empty"><h3>Comece o seu primeiro website</h3><p class="muted">Crie um projeto e depois poderá entrar no editor visual.</p><button class="btn primary" id="empty-new">Criar o meu primeiro website</button></div>`}</div></section>`,'dashboard');const create=async()=>{const name=prompt('Nome do website:');if(!name?.trim())return;try{await api('/api/projects',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:name.trim()})});dashboard()}catch(e){alert(e.message)}};['new-site','new-site-2','empty-new'].forEach(id=>{const el=document.querySelector('#'+id);if(el)el.onclick=create})}catch(e){layout(`<section class="error"><h3>Não foi possível carregar o Dashboard.</h3><p>${esc(e.message)}</p></section>`,'dashboard')}}
async function account(){layout('<div class="loading">A carregar a sua conta...</div>','account');const u=await getAuth();if(!u){layout(`<section class="auth-gate"><div class="eyebrow">Minha conta</div><h1>Entre na sua conta Nexauren.</h1><p>O perfil, plano e billing do Builder usam a mesma identidade Nexauren.</p>${authLinks('#/account')}</section>`,'account');return}try{const b=await api('/api/billing/account'),a=b.account||{},s=b.subscription;billingSubscription=s||null;layout(`<section class="hero compact"><div class="eyebrow">Minha conta</div><h1>Olá, ${esc(u.username||'criador')}.</h1><p>Esta área usa a mesma conta e o mesmo billing do Nexauren.</p></section><section class="account"><aside class="profile"><div class="avatar">${esc((u.username||u.email||'N').slice(0,1).toUpperCase())}</div><strong>${esc(u.username||'Utilizador')}</strong><p>${esc(u.email||'')}</p><span class="status">Conta autenticada</span></aside><div class="card"><h2>Plano e conta</h2><div class="row"><span>Plano</span><strong>${esc(a.plan_name||a.plan_id||'Free')}</strong></div><div class="row"><span>Preço</span><strong>${(Number(a.price_minor||0)/100).toFixed(2)} ${esc(a.currency||'USD')}</strong></div><div class="row"><span>Créditos por ciclo</span><strong>${Number(a.credits_per_cycle||0).toLocaleString()}</strong></div><div class="row"><span>Estado da subscrição</span><strong>${esc(s?.status||'Sem subscrição')}</strong></div><div class="actions"><a class="btn primary" href="#/pricing">Ver planos</a><a class="btn secondary" href="#/dashboard">Dashboard</a><button class="btn secondary" id="account-logout">Sair</button></div></div></section>`,'account');document.querySelector('#account-logout').onclick=logout}catch(e){layout(`<section class="error"><h3>Não foi possível carregar a conta.</h3><p>${esc(e.message)}</p></section>`,'account')}}
async function faq(){layout('<div class="loading">A carregar FAQ...</div>','faq');try{const d=await api('/api/faq');layout(`<section class="hero compact"><div class="eyebrow">FAQ</div><h1>Perguntas frequentes</h1><p>Informações sobre conta, billing e Website Builder.</p></section><section class="faq-list">${(d.items||[]).map(x=>`<details class="card"><summary>${esc(x.question)}</summary><p>${esc(x.answer)}</p></details>`).join('')}</section>`,'faq')}catch(e){layout(`<section class="error"><h3>Não foi possível carregar a FAQ.</h3><p>${esc(e.message)}</p></section>`,'faq')}}
function legal(title,text,active){layout(`<section class="legal-page"><div class="eyebrow">Nexauren</div><h1>${title}</h1><div class="card"><p>${text}</p></div></section>`,active)}
function route(){const h=location.hash||'#/';if(h.startsWith('#/pricing'))pricing();else if(h.startsWith('#/dashboard'))dashboard();else if(h.startsWith('#/account'))account();else if(h.startsWith('#/faq'))faq();else if(h.startsWith('#/privacy'))legal('Privacidade','O Website Builder utiliza a conta Nexauren para autenticação e associa os projetos ao identificador da mesma conta.','');else if(h.startsWith('#/terms'))legal('Termos','Ao utilizar o Website Builder, concorda com as regras aplicáveis ao serviço Nexauren.','');else if(h.startsWith('#/auth'))authPage();else home()}
window.addEventListener('hashchange',route);route();